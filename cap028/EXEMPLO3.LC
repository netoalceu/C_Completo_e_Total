/* Programa #3 de demostra‡ao de Little C. */
/* Atribui‡oes como opera‡oes. */
main()
{
  int a, b;

  a = b = 10;

  print(a); print(b);
  while(a=a-1) {
    print(a);
    do {
      print(b);
    }while((b=b-1) > -10);
  }
}
